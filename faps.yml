name: faps
channels:
  - defaults
dependencies:
  - pip
  - pip:
    - faps
    - matplotlib
  - numpy==1.26
  - scipy
  - pandas
  - bottleneck>=1.3.6
  - python>=3.9
  - tqdm
  - ipykernel
  - pip:
    - faps

